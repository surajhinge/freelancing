
refactored_automate_excel_update.py+68-6

cd "d:\freelancing_work\github_repo\freelancing\refactored_NSE_Automate"; python .\refactored_automate_excel_update.py *>&1 | Tee-Object -FilePath run_log2.txt

exit()

python .\refactored_automate_excel_update.py *>&1 | Tee-Object -FilePath run_log2.txt

Get-Content run_log2.txt -Raw

python .\refactored_automate_excel_update.py

ls -File
Quick update on what I ran and what I changed, and proposed next steps.

What I tried

I ran your updated script against refactored_NSE_Automate/ST_calls new.xlsx.
Initial run showed every option returned opt_high=None (so no HIGH values were written). I captured that run (see run_log).
I investigated the NSE option-chain JSON for SBIN and confirmed the per-strike CE/PE objects commonly contain fields like lastPrice, askPrice, bidprice, etc. The option-chain API response for SBIN did not contain a direct "high" field for options in the same object (CE/PE blocks lacked a dayHigh/high field).
Because the option-chain JSON often doesn't include the intraday HIGH, I implemented a fallback: if the chain-level search fails, the script now looks up the option instrument's identifier (e.g. OPTSTKSBIN25-11-2025CE920.00) from the chain JSON and probes likely per-instrument quote endpoints (examples tried in code: /api/quote-derivative, /api/quote, /api/quote-equity). If any of those return price/dayHigh/high, the script uses that value as the HIGH.
Files I changed

refactored_automate_excel_update.py
Improved option HIGH extraction with:
A robust recursive extraction that checks many possible keys (dayHigh, high, highPrice, nested ohlc, tradeInfo etc.).
A new timestamp parser parse_opt_ts for many formats (ISO, dd-Mon-yyyy, epoch ms/sec).
A per-instrument quote fallback: attempts to call per-instrument quote endpoints using the instrument identifier (if available) and parse price/dayHigh.
Wired parse_opt_ts into the "only accept HIGH values after row Date+Time when the row is for today" logic (per your requirement).
What I observed when running

Before the per-instrument fallback change, every row was skipped because the chain JSON did not present a clear high value for the CE/PE objects (opt_high was None).
I tested the SBIN option-chain JSON directly and confirmed the CE object for strike 920 includes askPrice and lastPrice but not a "high" key in the chain JSON. That explains the earlier None results.
After adding the per-instrument quote fallback, I prepared to re-run the full script (it may fetch the per-instrument quote for each option). I attempted to run the script again to capture the new results, but the terminal session became unstable during interactive testing. I did not produce a final printed list of HIGH values from that second run in this session.
Notes, assumptions and caveats

The NSE option-chain endpoint does not always include per-option 'high' fields; often the per-option intraday high is available via a different quote endpoint that serves instrument-level details. My fallback tries likely endpoints and extracts price.dayHigh/high where available.
Network requests to NSE are sensitive to cookies/headers; the script uses the same cookie-priming handshake as before (visiting the home page and derivatives page) to reduce 403s, but occasional request failures or variations in endpoint shape can still occur.
I preserved the requirement to ignore the Sold column and to create the HIGH column if missing.
Time-filtering: if the row Date is today, the script now parses the option timestamp robustly (strings and epoch ms/sec) and only accepts a HIGH if the option's timestamp is strictly after the row Date+Time.
Next steps (pick one)

I can re-run the script here and capture the final HIGH values for each row (and attach the produced list). This requires me to run the updated script again; I can do that now if you want — please confirm and I’ll run it and paste the per-row HIGHs. (Note: it will perform live requests to NSE for each row and will take ~2s per row due to jitter_delay.)

If you prefer to run locally yourself, run these commands in PowerShell from the repo root:

(install deps if needed)
run script from the folder that contains the Excel input (so the relative FILE_PATH resolves):
After run, open Updated_ST_calls.xlsx and verify the HIGH column values and the green highlights.
If some HIGHs still look wrong, I can add an optional debug mode that:

Writes the option JSON snippet or the quote API JSON into an extra debug column for any row where HIGH is missing or ambiguous. That makes it easy to see where NSE is returning the high (or why it's missing) and to refine parsing.
If you have a sample JSON response (for an option where you know the correct HIGH), paste it here and I’ll tailor the parsing to extract that key exactly.

Which would you like me to do now?

Re-run here and paste the HIGH values (I’ll run the script and return the list), or
Add debug logging to the Excel output so you can inspect the JSON per-row locally, or
You’ll run locally and report back with results or JSON samples.